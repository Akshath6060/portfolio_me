# Canonical public URL used for SEO metadata (no trailing slash).
VITE_SITE_URL=https://akshath-ok.vercel.app

# Optional. Server-only token used by /api/projects to increase GitHub limits.
GITHUB_TOKEN=
